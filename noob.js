const yesBtn=document.getElementById('yesBtn');const noBtn=document.getElementById('noBtn');const area=document.getElementById('buttonArea');const tease=document.getElementById('tease');const messages=['Nice try 😂','Too slow!','Nope 😭','You can\'t catch me!','Try again 😏','I\'m faster than you!','Bro really tried 💀','Almost!','Not happening 😂'];function moveYesButton(){const a=area.getBoundingClientRect(),b=yesBtn.getBoundingClientRect();const maxX=Math.max(0,a.width-b.width),maxY=Math.max(0,a.height-b.height);yesBtn.style.left=Math.random()*maxX+'px';yesBtn.style.top=Math.random()*maxY+'px';tease.textContent=messages[Math.floor(Math.random()*messages.length)]}yesBtn.addEventListener('mouseenter',moveYesButton);area.addEventListener('mousemove',e=>{const r=yesBtn.getBoundingClientRect();const d=Math.hypot(e.clientX-(r.left+r.width/2),e.clientY-(r.top+r.height/2));if(d<90)moveYesButton()});yesBtn.addEventListener('touchstart',e=>{e.preventDefault();moveYesButton()},{passive:false});noBtn.addEventListener('click',()=>{window.location.href='correct.html'});